

const Uri = new URL('https://api.first.org/data/v1/countries');

export declare interface Country {
  name: string,
  code: string,
}

const search = (predicate: string): Country[] => {
  uri.
  fetch()
}

export default search;

